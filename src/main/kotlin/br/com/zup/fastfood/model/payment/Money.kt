package br.com.zup.fastfood.br.com.zup.fastfood.model.payment

import br.com.zup.fastfood.utils.*

class Money {
/*    var moneyPay = 0.0
    val cartTotalValue = Utilities.cartTotalValue
    var changePayment = 0.0

    var moneyValue = 0.0
        set(value) {
            if(value > 0){
                field = value
            } else {
                println(NEGATIVE_VALUE_MSG)
                ()
            }
        }

    fun insertMoneyValueZ(): Double {
        return try {
            moneyValue = readln().toDouble()
            return moneyValue
        } catch (ex: IllegalArgumentException) {
            println(INVALID_VALUE)
            insertMoneyValueZ()
        }
    }

    fun paymentMessage(
        changePayment: Double = moneyValue - cartTotalValue
                insertMoneyValueZ()

            if (Utilities.cartTotalValue > moneyValue){
            println(MISSING_VALUE_MSG+${cartTotalValue-moneyValue})
        } else if (Utilities.cartTotalValue == moneyValue){
            println(SUCCESS_PAYMENT)
        } else {
            changePayment = moneyValue - cartTotalValue
            println(CHANGE_VALUE_MSG)
        }
    )

    val insertFormattedValue = Utilities.insertFormattedValue(moneyPay)*/
}
